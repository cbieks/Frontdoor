# Frontdoor internal dashboard, design context

Register: **product**. Brand: **Frontdoor's own** (shared with the marketing site).
Goal: a clean, fast, Linear / Raycast-tier operator tool where design serves the
work rather than performing.

The dashboard already exists under `app/dashboard/`, so its context is best
generated by `/impeccable document` (scan the real code), then refined with
`/impeccable audit` and `/impeccable polish`. Set
`IMPECCABLE_CONTEXT_DIR=docs/design/dashboard` for this surface.
